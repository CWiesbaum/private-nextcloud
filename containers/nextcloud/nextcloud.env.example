# Nextcloud Container Environment Variables
# Copy this file to .env and customize as needed

# Container Configuration
NEXTCLOUD_HTTP_PORT=8080
NEXTCLOUD_HTTPS_PORT=8443

# Volume Directories
NEXTCLOUD_CONFIG_DIR=/var/lib/nextcloud/config
NEXTCLOUD_DATA_DIR=/var/lib/nextcloud/data

# LinuxServer.io Image Configuration
# These will be passed to the container
PUID=1000
PGID=1000
TZ=Etc/UTC

# Database Configuration (for future use with separate DB container)
# DB_TYPE=mariadb
# DB_HOST=nextcloud-db
# DB_PORT=3306
# DB_NAME=nextcloud
# DB_USER=nextcloud
# DB_PASSWORD=changeme

# Redis Configuration (for future use with Redis container)
# REDIS_HOST=nextcloud-redis
# REDIS_PORT=6379
